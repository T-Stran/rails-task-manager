<h1>My Tasks</h1>

<ul>
  <% @tasks.each do |task| %>
    <li><%= check_box("tag", tag.id, {checked: task.completed}) %><%= link_to task.title, task_path(task.id)%></li>
  <% end %>
</ul>

<%= link_to "+ Add a new task", new_task_path %>
